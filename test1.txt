(setq a (+ 5 3))

(setq x (not (or T F)))

(setq day 2)
(case day
(1 (case day
    (1 (case day
    (1 (setq b 10)))))
)
(4 (setq c 9))
(3 (if (> c 16)
    (case day
    (1 (setq s 4)))))
(7 (case day
    (1 (setq s 4))))
)

(if (> c 16)
    (setq result 0)
 
)

(setq b (+ a 10))
(setq c b)
(if (>= c 16)
    (setq result 0)
    (setq result 1)
)

( print "Value of result is" )
(print result)

