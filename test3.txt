(setq start1 0)
(setq end1 10)
(setq start2 0)
(setq end2 10)
(loop while (< start1 end1)
    (loop while (< start2 end2)
        (setq result (+ start1 start2))
        (setq start2 (+ start2 1))
        (print result)
    )
    (setq start1 (+ start1 1))
)
